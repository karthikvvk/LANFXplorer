# LANFXplorer — 32-bit dependencies
# Python 3.10.11 required (aioquic needs >=3.10 for win32 wheel)
#
# NOTE: bcrypt is OMITTED — no 32-bit wheel exists.
#       Use bcrypt_compat.py (PBKDF2 fallback) instead.

aioquic>=1.3,<2.0
paramiko>=3.0
requests>=2.28
flask>=2.3
python-dotenv>=1.0
elevate>=0.1
scapy>=2.5
# NOTE: ownca is OMITTED — it requires cryptography<35,
#       but aioquic requires cryptography>=42. Incompatible.
#       The pki/ module uses cryptography/pyOpenSSL directly.
cryptography>=42.0,<46.0
pyOpenSSL>=23.0
service-identity>=23.1
keyring>=24.0
